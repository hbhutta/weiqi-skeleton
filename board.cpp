#include <board.h>

/**
 * @brief "Places" a stone on the board by setting the coordinates to a stone
 * @note The board must be initialized before any stone is placed on it
 * @return
*/
void Board::placeStone(Stone stone, int x, int y) {
    
    
}